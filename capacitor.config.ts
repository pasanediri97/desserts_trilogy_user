import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'desserts_trilogy_user',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
